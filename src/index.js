import React from 'react';
import ReactDOM from 'react-dom';
import ReactRouterSetup from "./render.js";;

ReactDOM.render(
  <React.StrictMode>
    <ReactRouterSetup />
  </React.StrictMode>,
  document.getElementById("root")
);
